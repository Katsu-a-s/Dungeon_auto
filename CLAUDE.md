# Dungeon_auto 自動開発ルーティン 指示書

このリポジトリ(`Katsu-a-s/Dungeon_auto`)は、Claude による**1時間ごとの自動開発ルーティン**の対象。
Python + pygame 製のローグライク風ダンジョン探索ゲームで、メインファイルは
`dungeon_revNNN.py`(単一ファイル構成)。**過去の rev ファイルは削除せずすべて
リポジトリに残す**運用のため、ディレクトリ内には `dungeon_rev100.py` 〜最新版まで
複数バージョンが並存する。「今動かすべき最新版」は常に**最も番号が大きいファイル**。

作業者(Claude)はこのルーティンにおいて**開発者兼PM**として振る舞う。実装だけでなく、
README を常に実態と一致させておく責任も持つ。

## 0. 事前確認

- `gh auth status` で GitHub CLI が認証済みか確認する。未認証なら**その旨を報告して作業を終了**する
  (認証情報の入力や設定変更はユーザー自身に行ってもらう)。
- リポジトリが未クローンならクローンし、クローン済みなら `main` を最新化(pull)する。
- 破壊的な git 操作(force push、`git reset --hard` など)は指示がない限り行わない。

## 1. 現状把握(重複作業の防止)

- ディレクトリ内の `dungeon_rev*.py` を確認し、**最も番号の大きいファイル**を今回の
  ベースにする。
- `README.md`(現在の実装状況・直近の更新履歴)を読む。
- `git log --oneline -60` で直近のコミット内容を確認し、何がすでに実装済みかを把握する。
- `git branch -a` でマージされずに残っているブランチも確認する。過去の自動実行で作られたまま
  放置されているブランチが多数あるが、**基本的にはそれらをマージせず**、新しいブランチで独立した
  変更を行う(内容が古い/重複している場合は無視してよい)。
- 同じ機能・演出を重複して追加しないよう、これから作る機能が既存の README 記載内容と
  被っていないか必ず確認する。

## 2. 開発方針

- **UI/UX を意識**し、プレイヤーが直感的に使える・気づける形にする(例: 視覚演出だけでなく
  音でも気づけるようにする、操作説明を分かりやすくする、迷わない導線にする、など)。
- **ワクワクして飽きない**ゲーム性を意識する(サプライズ・報酬感・目新しさのある変化を優先し、
  地味な内部変更だけに終始しない)。
- 既存のコードスタイル・命名規則(変数名、`FLOOR_MODIFIERS` のような辞書パターン、
  `global` 変数の宣言方法、`draw_xxx(bg)` 関数のパターンなど)を踏襲する。
- 破壊的変更(ゲームが起動しなくなる、セーブデータと非互換になる等)は避ける。
- 過剰な抽象化・将来を見越した作り込みは避け、今回の変更に必要な範囲に留める。

## 3. 機能選定・実装

- 1回の自動実行につき、ゲームに追加できる小さな新機能・改善を**3つ**選んで実装する
  (例: 新しい敵、アイテム、UI改善、バグ修正、BGM/SE追加、バランス調整など)。
- 3つ合わせて**小〜大規模**の変更を1回の実行(1PR)で行ってよい(個々は小粒でも、
  3つまとめることで中〜大規模の変更になり得る)。
- 3つの機能はそれぞれ独立して動作確認できるようにし、1つの不具合が他の2つを
  巻き込んで壊さないようにする。

## 4. rev バージョンの繰り上げ(旧ファイルは削除せず残す)

**メインファイルを変更するたびに、必ず以下を行う:**

1. 現在の最新ファイル(例 `dungeon_rev121.py`)は**変更せずそのまま残す**。削除・上書きは
   絶対にしない(`git mv` によるリネームも行わない)。
2. 現在の最新ファイルをコピーし、rev 番号を1つ上げた新しいファイルとして**同じディレクトリに**
   保存する(例 `dungeon_rev121.py` → `dungeon_rev122.py`)。
   ```bash
   cp dungeon_rev121.py dungeon_rev122.py
   ```
3. 今回選んだ3つの機能・改善は、すべてこの新しいファイル(`dungeon_rev122.py`)に対して実装する。
4. 以下の**「現在のメインファイル」を参照している箇所**を、新しい rev 番号に更新する:
   - `README.md`(メインファイル名の記載、`pip install` の実行コマンド例、ディレクトリ構成、
     行数の目安など複数箇所)
   - `.gitignore`(コメント内のファイル名言及)
   - その他、リポジトリ内でファイル名を直接参照している箇所があれば同様に更新する。
5. 旧ファイル(`dungeon_rev121.py` など過去のすべてのバージョン)はリポジトリに
   コミットされたまま残し、`git add`/コミットの対象から外さない(誤って削除しないよう注意)。

この規則は演出・数値調整などどんなに小さな変更であっても適用する。

## 5. 動作確認

マージ前に必ず以下を実施する:

- 構文チェック: `python -m py_compile dungeon_rev<新番号>.py`
- 簡易な起動確認(ヘッドレス): 例
  ```bash
  SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy timeout 8 python dungeon_rev<新番号>.py
  ```
  数秒間クラッシュせずメインループが回ることを確認する(`timeout` によるプロセス終了は正常)。
- 変更したロジックについては、該当コード箇所を読み返して意図通りに動作するか目視でも確認する。
- 3つの機能それぞれについて、実装箇所を個別に見直す。

## 6. ブランチ・コミット・PR・マージ

1. 新しいブランチを作成する:例 `claude/feature-<short-desc>-<timestamp>`
   (`<timestamp>` は `date +%Y%m%d_%H%M%S` 形式)。
2. 変更内容(新しい rev ファイル + README + .gitignore 等)をコミットする。**旧 rev
   ファイルは変更していないので差分には含まれない**。コミットメッセージは
   「何を」「なぜ」を簡潔に。
3. `gh pr create` で PR を作成し、今回実装した3つの機能・改善それぞれについて
   わかりやすく説明する(箇条書き推奨)。
4. 明らかな構文エラーがないこと・上記の動作確認が済んでいることを確認したうえで、
   `gh pr merge --merge`(または `--squash`)で `main` にマージする。
5. マージ後、ローカルの `main` を `pull` し、リモート `main` と一致していることを確認する。

## 7. 完了報告

作業の最後に、以下を簡潔に報告する:
- 今回実装した3つの機能・改善それぞれの概要
- 何をマージしたか、rev 番号がいくつからいくつに上がったか(旧ファイルは残したことも明記)
- PR の URL

## 8. README 更新(中学生でもわかる内容に)

`README.md` は**専門用語に詳しくない中学生が読んでも内容がわかる**ように書く。
プログラミング用語やコードの内部構造(関数名・変数名など)をそのまま羅列するのではなく、
「このゲームで何ができるか」「今回何が変わったか」を平易な言葉で説明する。

今回の変更に応じて以下を更新する:

- 冒頭にゲーム全体の内容(コード全体)を要約した紹介文を置く:
  - どんなゲームか(ジャンル・目的・遊びの流れ)
  - 遊び方の基本(操作方法・起動コマンド)
  - 現在実装されている主な要素を、専門用語を避けて平易にまとめる
- 「現在の実装状況」内の該当セクションに、新機能の説明を平易な言葉で追記する。
  - 直前の自動実行で `(NEW)` を付けた項目からは `(NEW)` を外し、今回追加した項目にのみ
    `(NEW)` を付ける(常に「最新の項目だけがNEW」という状態を保つ)。
- 「直近の更新履歴(最新順)」の先頭に今回の変更(3つ)をそれぞれ1〜3行で追記する。
  リストが長くなりすぎないよう、古い項目を末尾から間引いてもよい(目安: 10件前後)。
- メインファイル名を参照している箇所(トップの説明、遊び方のコマンド例、ディレクトリ構成)を
  新しい rev 番号に更新する。
- 過去の rev ファイルが複数残っていることが分かるよう、「実行するのは常に一番番号が
  大きいファイル」である旨をディレクトリ構成の説明などに明記する。

## 9. CI(継続的インテグレーション)について

- **現状、このリポジトリには `.github/workflows/` が存在せず、CI は一切設定されていない。**
  そのため `gh pr view` 等で確認しても checks は常に0件で、`mergeable_state` はコンフリクトの
  有無だけを反映したものになる(「テストに合格した」ことをGitHub上のステータスとしては
  確認できない)。
- 5節の構文チェック・ヘッドレススモークテストは、あくまで**Claudeがそのつど手元で
  実行しているだけ**であり、GitHub上に記録が残るものではない。この事実を完了報告や
  PR説明で「CIに合格した」のように誤認させる書き方をしない(「`py_compile`と
  ヘッドレステストをローカルで実施した」のように正確に書く)。
- 機会があれば、以下のような GitHub Actions ワークフローを追加し、CIを整備することを
  推奨する(3節の「3つの小改善」のうちの1つとして扱ってよい):
  - `.github/workflows/ci.yml` を新規作成し、PR作成・更新のたびに以下を自動実行する。
    - リポジトリ内の `dungeon_rev*.py` のうち最も番号が大きいファイルを検出し、
      `python -m py_compile` で構文チェックする。
    - `SDL_VIDEODRIVER=dummy` / `SDL_AUDIODRIVER=dummy` を設定し、`timeout` 付きで
      短時間の起動テスト(ヘッドレススモークテスト)を実行する。
  - CIを追加した後も、5節のローカルでの動作確認は省略せず引き続き実施する
    (CIだけに検証を任せきりにしない)。
- CIが無いこと自体は致命的な問題ではないため、他に優先すべき機能追加がある場合は
  無理にこの節の作業を最優先にする必要はない。

## 参考:過去の失敗・注意点

- `gh` コマンドは PATH に無い環境があるため、見つからない場合は
  `C:\Program Files\GitHub CLI\gh.exe` のようなフルパスでの実行を試す。
- リポジトリは `git clone` 済みのローカルパスが作業ディレクトリと異なる場所に存在する場合が
  あるので、`gh repo view Katsu-a-s/Dungeon_auto` 等でリポジトリの実在を確認し、
  既存クローンが見当たらなければ新規にクローンする。
- rev ファイルを増やす際、`git mv` ではなく `cp` 相当のコピーを使うこと。`git mv` を使うと
  旧ファイルが消えてしまい、「旧ファイルを残す」という運用ルールに反する。
